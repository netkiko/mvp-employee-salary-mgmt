import React from 'react';

import PageLayout from '../components/PageLayout';

const reports = (props) => {
    return (
        <PageLayout>
            <div>Reports</div>
        </PageLayout>
    );
};

export default reports;
